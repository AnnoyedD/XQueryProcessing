for $tuple in join(for $a in document("j_caesar.xml")/ACT,
					   $sspp in $a//SPEAKER
				   where $a/TITLE = "ACT II" 
				   return <tuple> {<a> {$a} </a>, <sspp> {$sspp} </sspp>} </tuple>,
				   for $a in document("j_caesar.xml")/ACT,
					   $sspp in $a//SPEAKER
				   where $a/TITLE = "ACT II" 
				   return <tuple> {<a> {$a} </a>, <sspp> {$sspp} </sspp>} </tuple>,
				  
				   [sp], [sspp]
				   )
return <result>{$tuple}</result>